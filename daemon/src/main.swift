


var message: String; // waht we receive from socket


func main() ->Integer {

    /* initialise server socket */
    /* initialise usb */
    /* listen for usb connections and disconnect */

    /* if usb connected send Operating System */
    /* send active app name */
    /* on change send active app name */

    return 0;
}